import { TableDataFormatDirective } from './tableDataFormat.directive';

describe('ConditionalFormatDirective', () => {
  it('should create an instance', () => {
    // const directive = new TableDataFormatDirective();
    // expect(directive).toBeTruthy();
  });
});
